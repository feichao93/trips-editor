.svg {
  position absolute
  left 0
  top 0
  width 100%
  height 100%
  background #eee
  > g {
    will-change transform
    transform-origin top left
  }
  
  polygon {
    fill-rule evenodd
  }
}
